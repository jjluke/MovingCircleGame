import simplegui




def draw(canvas):
    canvas.draw_polygon([(0, 0), (50, 0), (50, 50), (0,50)], 1, 'Blue')
    

frame = simplegui.create_frame("Home", 300, 200)
frame.set_draw_handler(draw)
frame.start()
